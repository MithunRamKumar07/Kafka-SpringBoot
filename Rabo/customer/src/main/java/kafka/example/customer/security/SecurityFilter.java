package kafka.example.customer.security;

/**<p> Filter class that performs security related checks </p>
 *
 */
public class SecurityFilter {
    // Intercept the requests to the Rest APIs and perform security checks.
    // This check is an extra layer of security on top of the Oauth check at the Gateway level.
}
